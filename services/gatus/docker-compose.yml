services:
  gatus:
    container_name: gatus
    image: twinproduction/gatus:latest
    restart: unless-stopped
    ports:
      - 8080:8080
    volumes:
      - ./config:/config
    environment:
      - TZ=Europe/Lisbon
      - GATUS_CONFIG_PATH=/config
